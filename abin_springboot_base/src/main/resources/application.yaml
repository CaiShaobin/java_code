user:
  hobby:
    - code
    - game
    - study
  id: 1
  name: abin
  pwd: 314159

server:
  port: 80
mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.abin.pojo
spring:
  datasource:
    url: jdbc:mysql://120.46.129.186:3306/abin_db?serverTimezone=UTC&useUnincode=true&characterEncoding=utf-8
    username: root
    password: abin_Java_202108
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
#    spring Boot 默认不注入这些属性，需要自己绑定
#    druid 数据源专用配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
#    配置监控统计拦截的filters，stat：监控统计、log4j：日志记录、wall：防御sql注入
#    如果允许报错 java.lang.ClassNotFoundException:org.apache.log4j.Priority
#    则导入log4j 依赖即可，Maven地址：https://mvnrepository.com/artifact/log4j/log4j
    filters: stat,wall,log4j
    maxPoolPreparedStatementPerCOnnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
    mvc:
      date-format: yyyy-MM-dd